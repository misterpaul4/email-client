export enum ProviderEnum {
  google = 'google'
}

export enum ConnectionType {
  oAuth = 'oAuth',
  appPassword = 'appPassword'
}

export enum ProviderStatus {
  active = 'active',
  inactive = 'inactive'
}
